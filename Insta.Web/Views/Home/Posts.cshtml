@model List<Insta.BusinessLogic.Entities.PostEntity>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @if (Model.Count == 0)
    {
        <span>Nothing here yet</span>
    }
</div>
